# ?? MemOS ? Global Context DB ???????

> ???2026-05-27  
> ??????? MemTensor/MemOS ??????????? Global Context DB ????? NAS ???????????????????????????? Agent ????????  
> ???????????????????????????????????????????

---

## 0. ??

MemOS ? Global Context DB ??????????LLM/Agent ????????????????????????????? Agent ???

?? Global Context DB ??????????????

1. ???? MemCube ??????????
2. ??????????? MemReader ??? intake/reader ??
3. improvement queue ????? scheduler??? retry?claim?worker?quota?recovery?
4. memory correction ???? quality report/promotion????????????
5. API/MCP/service ?????????? Components + Handlers ?????????
6. lifecycle ?????????????????????????????

??????????? endpoint?????????

---

## 1. MemOS ?????????

### 1.1 MemCube -> Context Cube

MemOS ? MemCube ????????????? memory??????????????????????

Global Context DB ?????

```text
Context Cube
```

???????

```text
context_cubes
cube_bindings
```

????

- user ????
- agent ????
- project ????
- session ????
- shared ?????
- asset/document ??????

#### ??????

```text
context_cubes
- id
- name
- cube_type          # user / agent / project / session / shared / kb
- owner_id
- visibility         # private / shared / public
- status             # active / archived
- created_by
- created_at
- updated_at
- metadata

cube_bindings
- id
- cube_id
- target_domain      # memory / document / asset / session / improvement
- target_id
- binding_kind       # owns / references / derived_from
- created_at
- metadata
```

#### ?????

??????? `user_id / agent_id / project_path / session_id`?????????????????? cube ????

- ? Agent ???????
- ??????????????
- ????/???????
- ???????????????

#### ??????

| MemOS | Global Context DB |
|---|---|
| MemCube | context_cubes |
| cube_id | cube_id |
| user/app isolation | cube_type + owner_id + visibility |
| composable KB | ? cube ?? |
| export/import | ?? cube snapshot/export/import |

---

### 1.2 MemReader -> Reader fast mode

MemOS ? MemReader ??? chat?document?image ?????????? memory item?

Global Context DB ?????document ingest?URL ingest?asset manifest?session event?tool trace?remember?????????????

#### ??????

???

```text
app/reader/service.py
app/reader/__init__.py
tests/test_reader.py
```

???? fast mode???? LLM?

```text
read_text_fast()
read_session_event_fast()
read_asset_manifest_fast()
read_tool_trace_fast()
```

?????

```text
ReaderItem
- source_domain
- source_id
- cube_id
- content
- content_kind        # fact / note / summary / chunk / trace / artifact_text
- tags
- confidence
- provenance
- metadata
```

#### ?????

```text
read_chat_fine()
read_document_fine()
read_asset_fine()
```

Fine mode ?? LLM ??????????????????????evidence quote ???

#### ?????

- ?? OCR/?????
- ?? PDF ???
- ??? embedding?
- ?? graph extraction?

?????????? worker?GCD ??? manifest ? artifact?

---

### 1.3 MemScheduler -> SQLite Scheduler

MemOS ???? ingestion?????????????????

Global Context DB ???? `improvement_tasks`?????????? scheduler?

#### ??????

?? `improvement_tasks`?

```text
retry_count
max_retries
next_run_at
claimed_at
claimed_until
worker_id
queue_name
last_error
```

???

```text
app/scheduler/service.py
tests/test_scheduler.py
```

???

```text
claim_next_task(queue_name, worker_id)
run_task(task_id)
release_expired_claims()
retry_failed_tasks()
run_pending_tasks(limit)
```

?????? executor?

```text
summarize_session
promote_session_memory
reindex_asset
refresh_asset_artifacts
resolve_missing_asset
verify_memory_evidence
refresh_stale_memory
resolve_memory_conflict
```

#### ?????

- Redis Streams
- ?? worker ??
- quota-based scheduling
- ??????
- ?????

??????? NAS ???????SQLite + ?? worker ???????

---

### 1.4 Memory Feedback -> ????

MemOS ? memory feedback ??????????????????????????????

Global Context DB ???? memory_evidence?promotion?quality_report?conflict detection?????????

```text
?????
?????
? A ?? B
??????
```

#### ??????

????

```text
memory_feedback
memory_feedback_actions
```

?????

```text
memory_feedback
- id
- cube_id
- feedback_text
- target_memory_id
- status             # pending / planned / applied / rejected
- created_by
- created_at
- metadata

memory_feedback_actions
- id
- feedback_id
- action_type        # update / archive / merge / add_evidence / create_memory
- target_memory_id
- payload
- status
- applied_at
- metadata
```

???

```text
POST /memory-feedback
GET /memory-feedback
POST /memory-feedback/{id}/apply
```

MCP?

```text
gcd_memory_feedback
gcd_list_memory_feedback
gcd_apply_memory_feedback
```

?????? LLM?????? memory_id ??? update/archive/add evidence?

---

### 1.5 Components + Handlers -> ???????

MemOS v2 ?? Components + Handlers???????????????

Global Context DB ?? `app/api.py`?`app/mcp_server.py`?`app/*/service.py`?`app/storage/repo.py`?? API/MCP ?????????? runtime?

#### ????

```text
app/runtime/components.py
app/runtime/__init__.py
tests/test_runtime_components.py
```

```text
RuntimeComponents
- settings
- sqlite
- vector_store
- embedder
- reader
- scheduler
- memory_service
- asset_service
- session_service
```

??????

```text
app/handlers/memory_handler.py
app/handlers/asset_handler.py
app/handlers/session_handler.py
app/handlers/cube_handler.py
app/handlers/scheduler_handler.py
app/handlers/reader_handler.py
```

???

- API/MCP ???????
- handler ??????
- service ??????
- repo ????

---

### 1.6 MemLifecycle -> ??????

MemOS ??????? memory lifecycle??????????????????

?? Global Context DB ? status?trust_level?evidence?stale detection?????? lifecycle ????

#### ???????

```text
candidate
active
verified
stale
archived
deleted
conflicted
```

?????

```text
reader_items
-> memory_candidates
-> review / auto-promote
-> memories
-> lifecycle check
```

?????

```text
memory_lifecycle_events
```

?? promoted?verified?refreshed?archived?corrected?merged?expired?

---

## 2. ??????????

### 2.1 Parametric Memory

?????????LoRA???????????????????? NAS/MCP ??????????????/?????

### 2.2 Activation Memory / KV Cache

?????Codex/OpenClaw/Claude Code ???? agent ?????? KV cache????????? plaintext memory?asset?session?scheduler?

### 2.3 Cloud Dashboard / ?????

??????????

1. ??/NAS ???
2. MCP/REST ???
3. ???????
4. ??? dashboard?

### 2.4 ??????

???? graph ????????????? graph database?????? SQLite metadata + LanceDB vector + evidence/binding/lifecycle ??

---

## 3. ???????

## Phase 1?Context Cube v0.1

???? Global Context DB ??????????

### ?? 1.1??? context_cubes / cube_bindings

???

```text
app/core/schemas.py
app/storage/repo.py
app/cubes/service.py
app/cubes/__init__.py
app/api.py
app/mcp_server.py
tests/test_cubes.py
```

???

- ?? cube?
- ?? cube?
- ?? cube?
- ??/?? cube?
- ????? cube?

REST?

```text
POST /cubes
GET /cubes
GET /cubes/{cube_id}
PATCH /cubes/{cube_id}
POST /cubes/{cube_id}/bindings
GET /cubes/{cube_id}/bindings
```

MCP?

```text
gcd_create_cube
gcd_list_cubes
gcd_get_cube
gcd_update_cube
gcd_bind_to_cube
gcd_list_cube_bindings
```

???

- ???? project/shared/private cube?
- ??? memory/session/asset/document ??? cube?
- diagnostics/db_counts ??? cube ???

### ?? 1.2??????? cube_id

???

```text
app/storage/repo.py
app/core/schemas.py
app/memory/service.py
app/assets/service.py
app/sessions/service.py
app/retrieval/service.py
app/control/service.py
tests/test_cubes.py
```

???

- memories ?? cube_id?
- assets ?? cube_id?
- sessions ?? cube_id?
- improvement_tasks / memory_promotion_proposals ?? cube_id?
- vector index ?? cube_id?
- search/recall ?? cube_id / cube_ids ???

???

- A cube search ??? B cube ?????
- ???? `[private_a, shared]` ???? shared ???
- ??????????

---

## Phase 2?Reader fast mode

??????? remember/ingest/session/asset ????? reader pipeline?

### ?? 2.1?ReaderItem + reader service

```text
app/reader/service.py
app/reader/__init__.py
app/core/schemas.py
tests/test_reader.py
```

???

```text
read_text_fast
read_session_event_fast
read_asset_manifest_fast
read_tool_trace_fast
```

???

- ????????? ReaderItem?
- ReaderItem ?? provenance?
- ReaderItem ??? memory promotion ? document chunk?

### ?? 2.2?????? reader

```text
app/control/service.py
app/ingest/pipeline.py
app/sessions/service.py
app/assets/service.py
tests/test_reader_integration.py
```

---

## Phase 3?SQLite Scheduler

???? improvement queue ??????? scheduler?

### ?? 3.1??? improvement_tasks ???

```text
app/storage/repo.py
app/core/schemas.py
app/improvements/service.py
app/scheduler/service.py
tests/test_scheduler.py
```

### ?? 3.2?????? executor

???

1. refresh_asset_artifacts
2. resolve_missing_asset
3. verify_memory_evidence
4. refresh_stale_memory
5. resolve_memory_conflict

---

## Phase 4?Memory Feedback

????????????

### ?? 4.1?feedback ???? apply

```text
app/memory/feedback_service.py
app/core/schemas.py
app/storage/repo.py
app/api.py
app/mcp_server.py
tests/test_memory_feedback.py
```

??????

- ?? memory_id ? update?
- archive?
- add evidence?
- ????? audit log?
- ???? memory version?

### ?? 4.2???????????

```text
feedback_text -> search_memory -> generate candidate actions -> review -> apply
```

???? LLM?

---

## Phase 5?Runtime Components + Handlers

????? `api.py` / `mcp_server.py` ?????

### ?? 5.1?RuntimeComponents

```text
app/runtime/components.py
app/runtime/__init__.py
tests/test_runtime_components.py
```

### ?? 5.2?? handlers

```text
app/handlers/memory_handler.py
app/handlers/asset_handler.py
app/handlers/session_handler.py
app/handlers/cube_handler.py
app/handlers/scheduler_handler.py
```

---

## 4. ?????

### ?????

1. Context Cube
2. Reader fast mode
3. Scheduler SQLite ???
4. Feedback ?????? apply

### ?????

1. Reader fine mode
2. LLM feedback action proposal
3. Redis Streams
4. dashboard
5. graph memory

### ?????

1. Parametric Memory
2. Activation Memory / KV cache
3. LoRA / model adaptation
4. ????????
5. ?? cloud ??

---

## 5. ????

MemOS ? Apache-2.0 ?????????????????????????????????????????????????????????

?????????????????????????????????
